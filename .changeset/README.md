# Changesets

This repo uses [Changesets](https://github.com/changesets/changesets) for versioning `@dre/tokens` and `@dre/components`.

```bash
npm run changeset        # describe your change
npm run version-packages # bump versions + changelogs
npm run release          # publish (requires npm auth)
```
